import { LinkBackgroundDirective } from './link-background.directive';

describe('LinkBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
